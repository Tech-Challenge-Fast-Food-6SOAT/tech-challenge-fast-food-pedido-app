apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedido-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pedido-app
  template:
    metadata:
      labels:
        app: pedido-app
    spec:
      containers:
        - name: pedido-app
          image: danilocassola/fiap-lanchonete-pedido:v1.1
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 100m
          env:
            - name: MONGODB_URL
              valueFrom:
                secretKeyRef:
                  name: pedido-app-secrets
                  key: MONGODB_URL
